# Cache purging

Cached PHP pages are automatically purged after a [successful deployment](../deployments/directories.md#successful-deployments).

For control over which responses get cached in the first place, see [cache patterns](patterns.md).